package com.chimera.rpg.ui.theme

/**
 * Defines inventory categories for filtering items in the inventory UI.
 * This enum enables category-based organization and filtering of inventory items.
 */
enum class InventoryCategory {
    ALL,
    EQUIPMENT,
    CONSUMABLES,
    MATERIALS,
    QUEST_ITEMS,
    TRINKETS
}